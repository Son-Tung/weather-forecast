OPENWEATHER_API_KEY=your_api_key_here
# Use your own API key from OpenWeatherMap. You can sign up for a free account and get an API key at https://openweathermap.org/api.
# Note: Do not share your API key publicly, as it can be misused by others. Keep it secure and do not commit it to version control systems.
# Example usage in code:
# import os
# api_key = os.getenv("OPENWEATHER_API_KEY")    
# Make sure to replace "your_api_key_here" with your actual API key before running the application.
# This .env file is used to store environment variables that can be accessed in your application code. It helps keep sensitive information like API keys secure and separate from your codebase.